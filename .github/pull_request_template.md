## What & Why

<!-- What problem does this PR solve? Why now? -->

## Scope

<!-- Affected files / modules / user-visible behavior changes -->

## Verification

- [ ] `make test` passes
- [ ] If roles or templates changed: line counts still within limits (roles ≤ 120 / templates ≤ 80 / rules ≤ 100)
- [ ] If knowledge templates changed: `scripts/bootstrap.sh` runs cleanly
- [ ] If adapter changed: `adapters/claude-code/install.sh` runs cleanly

## Changelog

<!-- Suggested entry for CHANGELOG.md [Unreleased] section — maintainer will merge at release time -->
